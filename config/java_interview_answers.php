<?php

return [
    'What is a class in Java?' => 'A class is a blueprint that declares the fields and methods shared by its objects. It defines a reference type; memory for instance fields is allocated when an object is created.',
    'Why do we need a class?' => 'A class groups state and behaviour into one reusable type. It provides encapsulation, supports inheritance and polymorphism, and lets a program create multiple objects with the same contract.',
    'What is the difference between a class and an object?' => 'A class is the type definition; an object is a runtime instance of that class. For example, `Car` can be a class while `new Car()` creates an object.',
    'Can a Java class exist without creating an object?' => 'Yes. A class can be loaded and its static members used without an instance. Abstract classes and utility classes may also exist without direct object creation.',
    'What are instance variables?' => 'Instance variables are non-static fields declared in a class. Every object has its own field values, and Java initializes them to default values before the constructor runs.',
    'What are instance methods?' => 'Instance methods run against a particular object, receive an implicit `this` reference, and can access both instance and static members.',
    'What are class variables?' => 'Class variables are fields declared with `static`. One value belongs to the class and is shared by all instances; access them through the class name.',
    'What is a nested class?' => 'A nested class is declared inside another class. It can be static or non-static and is useful when the nested type belongs closely to its enclosing type.',
    'What is an inner class?' => 'An inner class is a non-static nested class. Each instance is associated with an enclosing object and can access that object’s members, including private members.',
    'What is an anonymous class?' => 'An anonymous class is an unnamed, one-use class expression that extends a class or implements an interface. Lambdas are usually clearer for functional interfaces.',
    'Can a top-level class be private?' => 'No. A top-level class can be public or package-private. A nested class may be private, protected, public, or package-private.',
    'What is an abstract class?' => 'An abstract class cannot be instantiated directly. It may contain abstract methods plus implemented methods, fields, and constructors for subclasses to share.',
    'What is an object in Java?' => 'An object is a runtime instance of a class with identity, state, and behaviour. Variables normally hold a reference to the object rather than the object itself.',
    'How do you create an object?' => 'Commonly with `Type value = new Type(arguments);`. Objects can also be produced by factories, cloning, reflection, or deserialization.',
    'Where is an object stored in Java memory?' => 'Ordinary objects are generally allocated on the managed heap. A local reference may be in a stack frame; the JVM can optimize placement when escape analysis proves it safe.',
    'What is the difference between an object and a reference?' => 'The object contains runtime state; a reference is a value used to locate that object. Several references may point to the same object, and a reference may be `null`.',
    'Can multiple references point to the same object?' => 'Yes. Assigning one reference to another copies the reference, not the object. A mutation through either reference is visible through the other.',
    'What happens when an object reference is null?' => '`null` means the reference points to no object. Dereferencing it to access a field or method throws `NullPointerException`.',
    'What is object initialization?' => 'Initialization assigns an object’s initial state. Java applies default field values, runs field initializers and instance initializer blocks, then executes the constructor.',
    'What is the lifecycle of an object?' => 'An object is allocated, initialized, used while reachable, and becomes eligible for garbage collection when no live path can reach it. Collection timing is not guaranteed.',
    'What is garbage collection?' => 'Garbage collection automatically reclaims heap memory occupied by unreachable objects. It manages memory, but resources such as files and sockets still require explicit closing.',
    'What is shallow copy?' => 'A shallow copy creates a new outer object but copies field values as-is, so referenced mutable objects remain shared.',
    'What is deep copy?' => 'A deep copy duplicates the outer object and the mutable objects it owns, so changes to the copied graph do not affect the original.',
    'What is the difference between == and equals()?' => 'For primitives, `==` compares values; for references, it compares identity. `equals()` compares logical equality when the class overrides it. Equal objects must have equal hash codes.',
    'What is a constructor?' => 'A constructor initializes a new object. It has the class name, no return type, and runs after allocation when `new` is used.',
    'Why do we use a constructor?' => 'Constructors establish valid initial state and enforce required dependencies or invariants at object creation time.',
    'Does a constructor have a return type?' => 'No, not even `void`. Writing a return type creates an ordinary method with the same name as the class.',
    'Can a constructor be private?' => 'Yes. A private constructor restricts creation to the class itself, as used by factories, utility classes, and some Singleton implementations.',
    'What is a default constructor?' => 'If a class declares no constructor, the compiler supplies a no-argument constructor that calls `super()`. Once any constructor is declared, it is no longer supplied.',
    'What is a parameterized constructor?' => 'A parameterized constructor accepts values needed to initialize the object, such as `User(String name)`.',
    'What is constructor overloading?' => 'Constructor overloading declares multiple constructors with different parameter lists. Overload resolution occurs at compile time.',
    'What is constructor chaining?' => 'Constructor chaining invokes another constructor using `this(...)` or a superclass constructor using `super(...)`, reducing duplicate initialization.',
    'What is this()?' => '`this(...)` calls another constructor in the same class and must be the first statement in the constructor.',
    'What is super()?' => '`super(...)` calls a superclass constructor and must be first. If omitted, Java inserts `super()` when an accessible no-argument parent constructor exists.',
    'Can constructors be inherited?' => 'No. A subclass declares its own constructors, though each must ultimately invoke a superclass constructor.',
    'How are private constructors used in the Singleton pattern?' => 'A private constructor blocks outside creation; a static method or field exposes the controlled instance. Enum singletons are often safer for serialization and reflection.',
    'What are access modifiers?' => 'Access modifiers control visibility: `private` within the declaring class, package-private within the package, `protected` within the package and qualifying subclasses, and `public` everywhere.',
    'What is public access?' => 'A public member is accessible from any class when its enclosing type is also accessible.',
    'What is private access?' => 'A private member is accessible only within its declaring top-level class and its nested types.',
    'What is protected access?' => 'A protected member is accessible in the same package and from subclasses, with additional reference restrictions for subclasses in another package.',
    'What is package-private access?' => 'With no modifier, a type or member is accessible only to code in the same package.',
    'Which modifier is most restrictive?' => '`private` is the most restrictive ordinary access level.',
    'Can a method or constructor be private?' => 'Yes. Private methods hide implementation details; private constructors control object creation. Private methods are not overridden.',
    'What is the difference between private and protected?' => '`private` restricts access to the declaring class; `protected` also permits same-package access and controlled subclass access.',
    'Why should fields usually be private?' => 'Private fields preserve encapsulation, letting the class validate changes, maintain invariants, and change its internal representation without breaking callers.',
    'What is the static keyword?' => '`static` makes a member belong to the class rather than an instance. Static members exist independently of any particular object.',
    'What is a static variable?' => 'A static variable has one class-level value shared by all instances and is initialized when the class is initialized.',
    'What is a static method?' => 'A static method is invoked through the class and has no `this` reference. It can directly access only static members.',
    'Can a static method access instance data directly?' => 'No. It needs an object reference to access instance fields or methods because it has no current `this` object.',
    'Can a static method be overridden?' => 'No. Static methods are resolved by the reference type and can only be hidden by another static method with the same signature.',
    'What is method hiding?' => 'Method hiding occurs when a subclass declares a static method with the same signature as a superclass static method. The compile-time type selects the method.',
    'Can a static method be overloaded?' => 'Yes. Static methods can have the same name with different parameter lists, and overload selection occurs at compile time.',
    'What is a static block?' => 'A static initializer block performs class-level initialization that needs statements or exception handling.',
    'When is a static block executed?' => 'It runs once when the JVM initializes the class, in source order with static field initializers.',
    'What is a static nested class?' => 'A static nested class has no implicit enclosing instance. It can directly access only static members of the enclosing class.',
    'What is exception handling?' => 'Exception handling uses `try`, `catch`, `finally`, `throw`, and `throws` to report and recover from abnormal conditions without mixing recovery into normal flow.',
    'What is the difference between Error and Exception?' => 'Both extend `Throwable`. Exceptions represent conditions applications may handle; Errors usually signal serious JVM or environment failures that applications should not catch routinely.',
    'What is Throwable?' => '`Throwable` is the root type for objects that can be thrown and caught. Its main subclasses are `Error` and `Exception`.',
    'What are checked and unchecked exceptions?' => 'Checked exceptions must be caught or declared. Unchecked exceptions extend `RuntimeException` and usually indicate programming or validation failures.',
    'What is the difference between throw and throws?' => '`throw` raises a specific throwable in code; `throws` declares possible exceptions in a method signature.',
    'Can try exist without catch?' => 'Yes, when it has a `finally` block or is a try-with-resources statement. An ordinary `try` needs at least one `catch` or `finally`.',
    'What is try-with-resources?' => 'It automatically closes declared `AutoCloseable` resources in reverse order, including when an exception occurs.',
    'What is AutoCloseable?' => '`AutoCloseable` defines `close()`. Types implementing it can be managed by try-with-resources.',
    'How do you create a custom exception?' => 'Extend `Exception` for a checked exception or `RuntimeException` for an unchecked one, and provide constructors that preserve a message and cause.',
    'What is exception propagation?' => 'An uncaught exception moves up the call stack until a matching catch block handles it; otherwise the thread terminates and its handler is invoked.',
    'What is a process?' => 'A process is an executing program with its own address space and operating-system resources. A JVM normally runs as one process.',
    'What is a thread?' => 'A thread is an independent path of execution within a process. Threads share heap memory but have separate stacks and execution state.',
    'What is multithreading?' => 'Multithreading runs multiple threads concurrently to improve responsiveness or throughput. Shared mutable state requires careful coordination.',
    'What is the difference between Thread and Runnable?' => '`Thread` represents and controls a thread; `Runnable` represents a task. Prefer submitting tasks to an executor instead of subclassing `Thread`.',
    'What is Callable?' => '`Callable<V>` is a task that returns a value and may throw checked exceptions. Executors return a `Future<V>` for it.',
    'What is Future?' => 'A `Future` represents a pending asynchronous result and supports waiting, cancellation, and completion checks. `CompletableFuture` supports composition.',
    'What is ExecutorService?' => '`ExecutorService` manages a thread pool and task queue. Submit tasks, then shut it down cleanly rather than creating a thread per task.',
    'What is synchronization?' => 'Synchronization provides mutual exclusion and memory visibility around shared state using monitors, locks, atomics, or concurrent collections.',
    'What is a race condition?' => 'A race condition occurs when an outcome depends on uncontrolled timing between threads accessing shared mutable state.',
    'What is deadlock?' => 'Deadlock occurs when threads wait forever for locks held by each other. Prevent it with consistent lock ordering, smaller critical sections, or timed locks.',
    'What is volatile?' => '`volatile` guarantees visibility and ordering for reads and writes of a field, but it does not make compound operations such as increment atomic.',
    'What is the difference between wait() and sleep()?' => '`wait()` must be called while owning the monitor and releases it; `sleep()` pauses the current thread without releasing held locks.',
    'What is ConcurrentHashMap?' => '`ConcurrentHashMap` is a thread-safe map designed for concurrent access. It avoids a single global lock and does not permit null keys or values.',
    'What is the difference between Runnable and Iterable?' => '`Runnable` represents executable work through `run()`; `Iterable<T>` represents a sequence that supplies an `Iterator<T>` for traversal.',
    'What is abstraction?' => 'Abstraction exposes essential operations while hiding implementation details, commonly through interfaces and abstract classes.',
    'What is encapsulation?' => 'Encapsulation keeps state and the operations that protect it together, normally using private fields and a controlled public API.',
    'What is inheritance?' => 'Inheritance lets a subclass reuse and specialize accessible superclass behaviour through `extends`. Java supports one class parent and multiple interfaces.',
    'What is polymorphism?' => 'Polymorphism lets code use a common supertype while runtime dispatch selects the overridden instance method for the actual object.',
    'What is method overloading?' => 'Overloading uses the same method name with different parameter lists. The compiler selects the overload; return type alone cannot distinguish it.',
    'What is method overriding?' => 'Overriding replaces an inherited instance method with a compatible implementation. Runtime dispatch selects it based on the object type.',
    'What is an interface?' => 'An interface defines a contract a class can implement. It may contain abstract methods plus default, static, and private methods.',
    'What is the difference between an abstract class and an interface?' => 'An abstract class can hold instance state and constructors and has one inheritance path. A class can implement multiple interfaces, which primarily define capabilities.',
    'What is an IS-A relationship?' => 'IS-A means subtype inheritance: a subclass or implementation can be used wherever its superclass or interface is expected.',
    'What is a HAS-A relationship?' => 'HAS-A means composition: one object contains or collaborates with another object as a field.',
    'When should composition be preferred?' => 'Prefer composition when behaviour should be assembled or replaceable and there is no true subtype relationship. It reduces coupling to a superclass implementation.',
    'How are arrays created and initialized?' => 'Use `new int[3]` for a default-initialized fixed-size array or `int[] a = {1, 2, 3}` for an initializer. Elements are accessed by zero-based index.',
    'What is the difference between an array and ArrayList?' => 'Arrays have fixed length and can hold primitives directly. `ArrayList` grows dynamically, stores reference types, and provides collection operations.',
    'How do multidimensional arrays work?' => 'Java multidimensional arrays are arrays of arrays. Rows can have different lengths, for example `int[][] grid = new int[3][]`.',
    'Why is String immutable?' => 'Immutability makes strings safe to share, cache, use as map keys, and pass across threads. Operations that appear to modify a string return a new string.',
    'What is the String pool?' => 'The string pool reuses interned string instances. Identical string literals usually share an object; use `equals()` rather than `==` for content.',
    'What is the difference between String, StringBuilder and StringBuffer?' => '`String` is immutable; `StringBuilder` is mutable and fast for single-threaded building; `StringBuffer` is synchronized and usually slower.',
    'How do equals() and compareTo() differ?' => '`equals()` tests logical equality. `compareTo()` supplies ordering: zero means equal in the ordering, while negative or positive values indicate position.',
    'What is the Java Collections Framework?' => 'It is a set of interfaces, implementations, and algorithms for groups of objects, including `List`, `Set`, `Queue`, and `Map`.',
    'What is the difference between List, Set and Map?' => '`List` is ordered and permits duplicates; `Set` stores unique elements; `Map` associates unique keys with values and is not a `Collection` subtype.',
    'ArrayList versus LinkedList?' => '`ArrayList` usually gives faster indexed access and better cache locality. `LinkedList` has cheap link changes once positioned but slow indexing and higher memory overhead.',
    'HashMap versus ConcurrentHashMap?' => '`HashMap` is not thread-safe and allows nulls. `ConcurrentHashMap` supports concurrent operations, weakly consistent iteration, and disallows null keys and values.',
    'Comparable versus Comparator?' => '`Comparable` defines a type’s natural order in `compareTo`; `Comparator` supplies an external or alternate ordering and can be composed.',
    'What is the Stream API?' => 'The Stream API builds lazy pipelines over data using operations such as filter, map, and reduce. A stream is consumed once and does not store elements.',
    'What are intermediate and terminal operations?' => 'Intermediate operations return another lazy stream, such as `filter`. A terminal operation, such as `collect`, triggers traversal and produces a result or side effect.',
    'What do map(), filter() and reduce() do?' => '`map` transforms elements, `filter` retains matching elements, and `reduce` combines elements into one result.',
    'When should parallel streams be avoided?' => 'Avoid them for small workloads, blocking I/O, order-sensitive or stateful logic, and shared mutable state. Measure performance before using them.',
];
