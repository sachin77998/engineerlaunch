<div class="intelligence-shell"><div class="intelligence-hero"><div><span class="intel-label" style="color:#bfdbfe">LATEST INTELLIGENCE</span><strong>What changed. Why it matters.<br>What it means for your career.</strong></div></div><div class="intel-grid"><section class="intel-panel"><span class="intel-label">QUICK TAKE</span><h2>What you need to know</h2><p>{{$article->excerpt}}</p><div class="intel-facts"><span>Career impact<b>{{strtoupper($article->career_impact_level)}}</b></span><span>Skills identified<b>{{$skills->count()}}</b></span><span>Matching jobs<b>{{number_format($relatedJobCount)}}</b></span><span>Market direction<b>Growing</b></span></div></section><section class="intel-panel"><span class="intel-label">INTELLIGENCE SCORE</span><div class="intel-score">{{$intelligence['overall']}}/100</div>@foreach(['Market'=>'market','Career'=>'career','Technology'=>'technology','Hiring'=>'hiring'] as $label=>$key)<div class="intel-row"><span>{{$label}} impact</span><strong>{{$intelligence[$key]}}</strong></div>@endforeach</section></div><section class="intel-section"><span class="intel-label">WHY IT MATTERS</span><h2>The bigger picture</h2><p>{{$article->industryInsights->first()?->analysis?:$article->industry_impact}}</p></section><section class="intel-panel"><span class="intel-label">CAREER INTELLIGENCE</span><h2>What does this mean for your career?</h2>@forelse($article->careerInsights as $row)<div class="intel-row"><span>{{$row->role}}</span><strong>↑ {{ucwords($row->impact_level)}}</strong></div>@empty @foreach($roles as $role)<div class="intel-row"><span>{{$role}}</span><strong>↑ {{ucfirst($article->career_impact_level)}}</strong></div>@endforeach @endforelse</section><section class="intel-section"><span class="intel-label">SKILLS TO WATCH</span><h2>Skills worth learning</h2><div class="intel-tags">@foreach($skills as $skill)<span>{{$skill}}</span>@endforeach</div></section><section class="intel-section"><span class="intel-label">WHAT SHOULD DEVELOPERS DO?</span><h2>Turn this signal into action</h2><div class="intel-row">1. Keep architecture, database, security, testing and debugging fundamentals strong.</div><div class="intel-row">2. Learn practical APIs, SDKs and backend integration.</div><div class="intel-row">3. Build cloud, containers, queues, caching and observability skills.</div><div class="intel-row">4. Design critical workflows with tested fallback mechanisms.</div></section><section class="intel-panel"><span class="intel-label">TECHNOLOGY STACK</span><div class="intel-tags">@foreach($skills->take(12) as $skill)<span>{{$skill}}</span>@endforeach<span>APIs</span><span>Docker</span><span>Kubernetes</span><span>Cloud</span><span>Security</span></div></section><section class="intel-cta"><h2>Jobs connected to this intelligence</h2><p>{{number_format($relatedJobCount)}} active roles match the skills and career signals identified here.</p><a href="{{route('home')}}#jobs">View related jobs →</a></section>@if($relatedJobs->isNotEmpty())<section class="intel-section"><span class="intel-label">LIVE OPPORTUNITIES</span><div class="intel-cards">@foreach($relatedJobs as $job)<article class="intel-card"><small>{{$job->company?->name}}</small><h3>{{$job->title}}</h3><p>{{$job->location}}</p>@if(filter_var($job->external_url,FILTER_VALIDATE_URL))<a href="{{$job->external_url}}" target="_blank" rel="noopener noreferrer nofollow">View role →</a>@endif</article>@endforeach</div></section>@endif @if($relatedArticles->isNotEmpty())<section class="intel-section"><span class="intel-label">RELATED INTELLIGENCE</span><div class="intel-cards">@foreach($relatedArticles as $related)<article class="intel-card"><h3>{{$related->title}}</h3><a href="{{route('news.show',$related->slug)}}">Read analysis →</a></article>@endforeach</div></section>@endif <section class="intel-panel"><span class="intel-label">SOURCE & VERIFICATION</span><h2>Credibility and transparency</h2><p><strong>Source:</strong> {{$article->source?->name??'Original source'}}<br><strong>Published:</strong> {{$article->published_at?->format('M d, Y H:i')}}<br><strong>Updated:</strong> {{$article->updated_at->format('M d, Y H:i')}}</p>@if(filter_var($article->source_url,FILTER_VALIDATE_URL)&&in_array(parse_url($article->source_url,PHP_URL_SCHEME),['http','https'],true))<a href="{{$article->source_url}}" target="_blank" rel="noopener noreferrer nofollow">Open original source ↗</a>@endif</section></div>
