<style>.sitebar{min-height:72px;background:#0b1933;color:#fff}.sitebar-inner{width:min(1160px,calc(100% - 32px));min-height:72px;margin:auto;display:flex;align-items:center;justify-content:space-between;gap:24px}.site-logo{display:block;padding:3px 9px;border-radius:8px;background:#fff}.site-logo img{display:block;width:145px;height:55px;object-fit:contain}.site-nav{display:flex;align-items:center;gap:20px;flex-wrap:wrap}.site-nav a{color:#cbd5e1;text-decoration:none;font-size:14px}.site-auth{padding:9px 14px;border:0;border-radius:8px;background:#2563eb;color:#fff!important;font-weight:750}.candidate-nav{display:flex!important;align-items:center;gap:8px}.candidate-nav img,.candidate-nav span{width:36px;height:36px;border-radius:50%;object-fit:cover;background:#2563eb;display:grid;place-items:center;color:#fff}@media(max-width:820px){.sitebar{padding:16px 0}.sitebar-inner{align-items:flex-start;flex-direction:column}}</style>
<header class="sitebar"><div class="sitebar-inner">@include('partials.logo')<nav class="site-nav"><a href="{{route('home')}}#jobs">Jobs</a><a href="{{route('home')}}#companies">Companies</a><a href="{{route('learning.index')}}">Learning</a><a href="{{route('practice')}}">Practice</a><a href="{{route('about')}}">About us</a><a href="{{route('contact')}}">Contact</a>@auth @if(auth()->user()->role_code===2||auth()->user()->role==='admin')<a href="{{route('admin.dashboard')}}">Owner Dashboard</a>@elseif(auth()->user()->role_code===0||auth()->user()->role==='employer')<a href="{{route('employer.dashboard')}}">HR Dashboard</a>@else @php($navProfile=auth()->user()->candidateProfile)<a class="candidate-nav" href="{{route('dashboard')}}">@if($navProfile?->avatar_url)<img src="{{$navProfile->avatar_url}}" alt="Profile">@else<span>{{strtoupper(substr(auth()->user()->name,0,1))}}</span>@endif<b>{{auth()->user()->name}}</b></a>@endif<form method="post" action="{{route('logout')}}" style="margin:0">@csrf<button class="site-auth">Logout</button></form>@else<a href="{{route('employer.login')}}">HR Login</a><a href="{{route('owner.login')}}">Owner Login</a><a class="site-auth" href="{{route('register')}}">Student Sign Up</a>@endauth</nav></div></header>
