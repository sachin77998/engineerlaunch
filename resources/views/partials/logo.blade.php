<a class="{{ $class ?? 'site-logo' }}" href="{{ route('home') }}" aria-label="{{ config('brand.name') }} home">
    <img src="{{ asset(config('brand.logo')) }}" alt="{{ config('brand.logo_alt') }}">
</a>
