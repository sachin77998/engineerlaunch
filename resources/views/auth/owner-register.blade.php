@extends('layouts.app')
@section('title','Register Owner — Ascendia')
@section('content')<div class="container py-5"><form class="card shadow-sm mx-auto p-4" style="max-width:520px" method="post" action="{{route('owner.register.store')}}">@csrf<h1 class="h3">{{$hasOwner?'Register another platform owner':'Create the first platform owner'}}</h1><p class="text-secondary">{{$hasOwner?'Create another secured owner account.':'Set up the first secured owner account for Ascendia.'}}</p><div class="alert alert-info"><strong>Role codes:</strong> HR = 0, Student = 1, Owner = 2. Owner role code 2 is assigned automatically by the server.</div>@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{$error}}</li>@endforeach</ul></div>@endif<label class="form-label">Full name <span class="text-danger">*</span></label><input class="form-control" name="name" value="{{old('name')}}" required><label class="form-label mt-3">Email <span class="text-danger">*</span></label><input class="form-control" type="email" name="email" value="{{old('email')}}" required><label class="form-label mt-3">Password <span class="text-danger">*</span></label><input class="form-control" type="password" name="password" minlength="8" required><label class="form-label mt-3">Confirm password <span class="text-danger">*</span></label><input class="form-control" type="password" name="password_confirmation" minlength="8" required><button class="btn btn-primary mt-4">Create Owner Account</button></form></div>@endsection
