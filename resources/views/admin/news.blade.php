@extends('layouts.site') @section('title','News Administration') @section('content')<section class="section"><div class="container"><h1>Latest Intelligence Administration</h1><form class="panel" method="post" action="{{route('admin.news.sources')}}">@csrf<h2>Add allowlisted RSS source</h2><input class="field" name="name" placeholder="Source name" required><input class="field" name="website" type="url" placeholder="Website"><input class="field" name="feed_url" type="url" placeholder="HTTPS RSS URL" required><button class="primary">Validate and save</button></form><h2>Moderation queue</h2>@foreach($articles as $article)<article class="panel" style="margin:12px 0"><b>{{$article->title}}</b> · {{$article->processing_status}} · {{$article->relevance_score}}<form method="post" action="{{route('admin.news.moderate',$article)}}" style="display:inline">@csrf @method('PATCH')<button name="action" value="publish">Publish</button><button name="action" value="reject">Reject</button></form></article>@endforeach {{$articles->links()}}<h2>Fetch logs</h2>@foreach($logs as $log)<p>{{$log->source?->name}} — {{$log->status}} — {{$log->items_created}} created / {{$log->duplicates}} duplicates</p>@endforeach</div></section>@endsection
