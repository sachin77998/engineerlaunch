<article class="industry-career-card"><h3>{{ $role->name }}</h3>
@if(isset($taxonomy['careerMatch']['matches'][$role->id]))<span class="industrial-tag">{{ $taxonomy['careerMatch']['matches'][$role->id]['reason'] }}</span>@endif
<table><caption style="text-align:left;font-weight:700;margin:10px 0">Role at a glance</caption><tbody><tr><th scope="row">Department</th><td>{{ $role->department->name }}</td></tr><tr><th scope="row">Typical backgrounds</th><td>@if($role->profile && $role->profile->qualifications)<ul>@foreach($role->profile->qualifications as $qualification)<li>{{ $qualification }}</li>@endforeach</ul>@else Varies by employer @endif</td></tr><tr><th scope="row">Industries</th><td>{{ $role->sectors->pluck('name')->join(', ') ?: 'Not mapped yet' }}</td></tr></tbody></table>
@if($role->aliases->isNotEmpty())<h4>Other names to recognise</h4><ul>@foreach($role->aliases as $alias)<li>{{ $alias->name }}</li>@endforeach</ul>@endif
<h4>Skills to compare</h4>@if($role->profile && $role->profile->skills)<ul>@foreach($role->profile->skills as $skill)<li>{{ $skill }}</li>@endforeach</ul>@else<p>Skills are not mapped for this role yet. Read the actual opening for requirements.</p>@endif
@if($role->processes->isNotEmpty())<h4>Related processes</h4><ul>@foreach($role->processes as $process)<li>{{ $process->name }}</li>@endforeach</ul>@endif
<p><strong>Next:</strong> Compare these career terms with the employer’s actual requirements.</p><a href="{{ route('industrial.index', array_merge(collect($filters)->except(['page','jobs_page','companies_page','company','search'])->all(), ['department'=>$role->department_id,'role'=>$role->id])) }}">View recorded openings &rarr;</a></article>